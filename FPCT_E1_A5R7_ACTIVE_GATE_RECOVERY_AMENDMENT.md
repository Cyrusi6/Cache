# FPCT-E1 A5R7 Active-Gate Recovery Amendment

> Decision: `APPROVED_PROSPECTIVE_AMENDMENT` by the user on `2026-08-15`
>
> Protocol: `fpct_e1_mechanism_audit_v14_a5r7_active_gate_recovery`
>
> Base execution: `b2e349990eecef2aeb870baf82bbc81340fd49d7`
>
> State: `PRE-NATURAL / COMMIT+PUSH PENDING`

## Purpose

A5R7 repairs one deterministic precomputation composition error. A5R6 loaded
the immutable A5R4 gate through its historical live-tree verifier. Because the
A5R6 five-field repair legitimately changed the successor prepare source, that
historical tracked-source map could never equal the successor tree. The result
was `A5R4_TRACKED_SOURCE_TEST_MAP_IS_STALE` before natural row one.

This failure is engineering-only. It does not change or inform the FPCT
hypothesis, operator, endpoint, pair, population, tokenizer, alignment,
candidate prior, threshold or claim.

## Terminal predecessor

The `b2e34999` UID, run root and controller root are permanently consumed.
They are `NO_RESUME / NO_RELAUNCH / NO_REPAIR / NO_REUSE / NO_CLEANUP`.
Their input root contains exactly the execution identity and canonical BLOCKED
receipt. No choice audit, geometry, sidecar, manifest or GO was published.
Natural rows read were zero; no model/checkpoint/GPU/training/E1-2 access
occurred.

## Frozen composition

The historical A5R4 gate remains byte immutable:

```text
recipe/eval_recipe/fpct_e1/e1_a5r4_setgid_mode_synthetic_gate.json
SHA256 = 0f3477575b8a75fb4b6028c0eeda19aa56885aca2fdb0addce0171a5e02c7ea8
```

The producer consumes it only as historical scientific evidence:

1. canonical non-symlink regular file;
2. exact whole-file SHA256;
3. strict v11 schema;
4. exact schema/choice-semantics/protocol/artifact/status;
5. recomputed historical evidence SHA.

It must not call the historical full live-tree verifier. A separate A5R7 gate
validates the current successor source.

## Current-source continuity

The A5R7 gate must prove:

- all ten A5R3 publication functions remain AST-identical;
- thirteen of fourteen A5R4 mode/publication functions remain AST-identical;
- only `_load_active_a5_synthetic_gate` may change, and its new AST is frozen;
- all A5R3/A5R4 behavior tests and mode-predicate static checks still pass;
- v9 choice binding stays exactly five fields while A5R3 publication remains
  independently verified;
- b2e34999 root/controller bytes and portable closure remain unchanged.

The exact production preflight must invoke `_load_active_a5_synthetic_gate()`
before natural row one and exercise byte, alias, schema, identity and historical
live-verifier negative controls.

## Execution boundary

Only after the A5R7 verbose synthetic/preflight suite passes, its gate is
generated and independently verified, and a new commit is pushed with clean
local/upstream equality may the controller derive a new snapshot, UID, run
root and A5R7 state root. The execution restarts from group 1 and imports no
b2e34999 artifact.

E1-2/E1-3 remains prohibited until the new root has worker exit zero, canonical
input-lock GO, no BLOCKED receipt, and a strict durable deep-verifier receipt.
A separate diagnostic/intervention pre-output lock is still required before
loading any of the six E0 checkpoints. E1-pilot and confirmatory data remain
sealed.
