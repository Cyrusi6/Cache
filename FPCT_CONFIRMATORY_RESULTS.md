# FPCT Confirmatory Results

Status: `NOT ENTERED — PRETRAINED GPU ENGINEERING HARD STOP`

The single-pair confirmatory performance experiment did not start. The frozen
synthetic GPU numerical gate passed, but the subsequent unlabeled pretrained
smoke failed activation, replicated-collapse and profiled host-synchronization
gates. By preregistration, the controller stopped before matched training.

Consequently:

- completed formal matched triplets: `0/12`;
- formal arm runs/checkpoints: `0/36`;
- model-selection releases: `0`;
- held-out releases: `0`;
- accuracy/correctness evaluations: `0`;
- `T`, `O`, `I`, `N`, sign-flip tests and hierarchical bootstrap: not computed;
- performance/mechanism classification: not available.

`GPU_ENGINEERING_BLOCKED` is an execution-gate classification, not evidence
that FPCT is mathematically valid/invalid or that task accuracy improves or
declines. The frozen structural-support, reference-oracle and CPU/HF
implementation results remain intact. Any recovery requires a new prospective
scientific revision and new image/run lock; the failed run cannot be patched or
resumed into formal training.
